# Desk screenshots

- **`desk-tour.gif`** — README hero; cycles all seven tabs (~2.2s each)
- **`0N-*.jpg`** — stills for the README gallery
- **`frames/`** — raw capture scratch (gitignored); rebuild GIF with `./scripts/make_desk_tour_gif.sh`

Capture with Chrome MCP at 1280×800, save PNGs as `frames/01-overview.png` … `07-ops.png`, then run the make script.
