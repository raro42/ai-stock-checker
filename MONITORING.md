# Deprecated — see README

Monitor profile notes are archived here to avoid dual entrypoints.

**Use instead:** [README.md](README.md) (`docker compose --profile monitor up` only if you want signals-only). Primary path remains **`intelligent-trader`** + desk.
